<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/white">

    <ImageView
        android:id="@+id/clg_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/hrc_pic"/>

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo2"
        android:layout_below="@+id/clg_image"
        android:layout_centerInParent="true"
        android:layout_marginTop="-60dp"/>

    <TextView
        android:id="@+id/hrc_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logo"
        android:layout_centerInParent="true"
        android:textAlignment="center"
        android:layout_marginTop="30dp"
        android:fontFamily="serif"
        android:text="HANSRAJ COLLEGE"
        android:textColor="#B04400"
        android:textSize="30sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/clg_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_hindi_new"
        android:layout_below="@+id/hrc_name"
        android:layout_marginTop="30dp"
        android:layout_centerInParent="true"/>

    <ProgressBar
        android:id="@+id/simpleProgressBar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="350dp"
        android:layout_height="10dp"
        android:layout_below="@+id/clg_logo"
        android:layout_centerInParent="true"
        android:layout_marginTop="70dp"
        android:max="100"
        android:progress="0"
        android:progressBackgroundTint="#ffffff"
        android:progressTint="#B04400"/>

</RelativeLayout>